
// Importing the material.dart file from the Flutter framework.
import 'package:flutter/material.dart';

// Defining a constant variable 'KPrimaryColor' with a value of Color(0xFFE9EEF4).
const KPrimaryColor = Color(0xFFE9EEF4);

// Defining a constant variable 'KSecondaryColor' with a value of Color(0xFF326789).
const KSecondaryColor = Color(0xFF326789);

// Defining a constant variable 'KThirdColor' with a value of Color(0xFF78A6CB).
const KThirdColor = Color(0xFF78A6CB);

// Defining a constant variable 'KFourthColor' with a value of Color.fromARGB(218, 221, 41, 25).
const KFourthColor = Color.fromARGB(218, 173, 21, 109);

// Defining a constant variable 'KFifthColor' with a value of Color(0xFF484848).
const KFifthColor = Color(0xFF484848);
